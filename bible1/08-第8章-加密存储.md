# 第 8 章：加密存储

---

## 8.1 完整的可运行示例

让我们通过一个完整的示例来演示 sfsDb 的加密存储功能：

```go
package main

import (
	"bytes"
	"fmt"
	"os"

	"github.com/liaoran123/sfsDb/storage"
)

func main() {
	fmt.Println("=== sfsDb 数据库实战 - 第 8 章：加密存储示例 ===")
	fmt.Println()

	dbPath := "./bible_examples_chapter08_db"
	cleanup(dbPath)
	defer cleanup(dbPath)

	fmt.Println("1. 生成测试密钥")
	masterKey := make([]byte, 32)
	for i := range masterKey {
		masterKey[i] = byte(i)
	}
	fmt.Println("   ✓ 密钥生成成功 (32字节)")
	fmt.Println()

	fmt.Println("2. 创建加密配置")
	encryptConfig := &amp;storage.EncryptionConfig{
		Enabled:   true,
		Algorithm: "AES-256-GCM",
		MasterKey: masterKey,
	}
	fmt.Println("   ✓ 加密配置创建成功")
	fmt.Println("   - 算法: AES-256-GCM")
	fmt.Println("   - 密钥长度: 32字节")
	fmt.Println()

	fmt.Println("3. 创建加密存储")
	encryptedStore, err := storage.GetDBManager().NewLevelDBStore(dbPath, nil, encryptConfig)
	if err != nil {
		fmt.Printf("创建加密存储失败: %v\n", err)
		return
	}
	defer encryptedStore.Close()
	fmt.Println("   ✓ 加密存储创建成功")
	fmt.Println()

	fmt.Println("4. 测试基本的 Put 和 Get 操作")
	testKey := []byte("sensitive_data")
	testValue := []byte("这是需要加密的敏感数据")

	err = encryptedStore.Put(testKey, testValue)
	if err != nil {
		fmt.Printf("存储加密数据失败: %v\n", err)
		return
	}
	fmt.Println("   ✓ 数据加密存储成功")

	retrievedValue, err := encryptedStore.Get(testKey)
	if err != nil {
		fmt.Printf("获取解密数据失败: %v\n", err)
		return
	}
	fmt.Printf("   ✓ 数据解密获取成功\n")
	fmt.Printf("   原始数据: %s\n", testValue)
	fmt.Printf("   解密数据: %s\n", retrievedValue)

	if bytes.Equal(retrievedValue, testValue) {
		fmt.Println("   ✓ 数据一致性验证通过")
	} else {
		fmt.Println("   ✗ 数据一致性验证失败")
	}
	fmt.Println()

	fmt.Println("5. 测试批量加密操作")
	batch := encryptedStore.GetBatch()
	testData := map[string]string{
		"user_1_password": "secret123",
		"user_1_email":    "user1@example.com",
		"user_1_phone":    "13800138000",
	}

	for k, v := range testData {
		batch.Put([]byte(k), []byte(v))
	}
	fmt.Println("   ✓ 批量操作准备完成 (3条记录)")

	err = encryptedStore.WriteBatch(batch)
	if err != nil {
		fmt.Printf("执行批量加密操作失败: %v\n", err)
		return
	}
	fmt.Println("   ✓ 批量加密操作执行成功")

	for k, v := range testData {
		retrieved, err := encryptedStore.Get([]byte(k))
		if err != nil {
			fmt.Printf("获取批量数据 %s 失败: %v\n", k, err)
			continue
		}
		if bytes.Equal(retrieved, []byte(v)) {
			fmt.Printf("   ✓ %s 解密验证通过\n", k)
		}
	}
	fmt.Println()

	fmt.Println("6. 测试删除加密数据")
	err = encryptedStore.Delete(testKey)
	if err != nil {
		fmt.Printf("删除加密数据失败: %v\n", err)
		return
	}
	fmt.Println("   ✓ 加密数据删除成功")

	_, err = encryptedStore.Get(testKey)
	if err == storage.ErrNotFound {
		fmt.Println("   ✓ 验证数据已删除")
	} else {
		fmt.Println("   ✗ 数据删除验证失败")
	}
	fmt.Println()

	fmt.Println("7. 测试密码派生密钥 (PBKDF2)")
	passwordConfig := &amp;storage.EncryptionConfig{
		Enabled:    true,
		Algorithm:  "AES-256-GCM",
		Password:   "my_strong_password_123",
		Salt:       []byte("random_salt_value"),
		Iterations: 100000,
	}

	passwordDBPath := "./bible_examples_chapter08_password_db"
	cleanup(passwordDBPath)
	defer cleanup(passwordDBPath)

	passwordStore, err := storage.GetDBManager().NewLevelDBStore(passwordDBPath, nil, passwordConfig)
	if err != nil {
		fmt.Printf("创建密码派生密钥存储失败: %v\n", err)
	} else {
		defer passwordStore.Close()
		fmt.Println("   ✓ 使用密码派生密钥的加密存储创建成功")
		fmt.Println("   - 使用 PBKDF2 密钥派生")
		fmt.Println("   - 迭代次数: 100,000")

		passwordTestKey := []byte("password_protected")
		passwordTestValue := []byte("使用密码保护的数据")
		err = passwordStore.Put(passwordTestKey, passwordTestValue)
		if err == nil {
			fmt.Println("   ✓ 密码派生密钥加密存储成功")
			retrieved, _ := passwordStore.Get(passwordTestKey)
			if bytes.Equal(retrieved, passwordTestValue) {
				fmt.Println("   ✓ 密码派生密钥解密验证通过")
			}
		}
	}
	fmt.Println()

	fmt.Println("8. 查看加密配置")
	wrapper, ok := encryptedStore.(*storage.EncryptedStoreWrapper)
	if ok {
		savedConfig := wrapper.GetEncryptionConfig()
		fmt.Println("   加密配置详情:")
		fmt.Printf("   - 启用: %v\n", savedConfig.Enabled)
		fmt.Printf("   - 算法: %s\n", savedConfig.Algorithm)
		fmt.Printf("   - 盐值长度: %d 字节\n", len(savedConfig.Salt))
	}
	fmt.Println()

	fmt.Println("=== 加密存储示例完成！===")
}

func cleanup(path string) {
	os.RemoveAll(path)
}
```

这段代码展示了 sfsDb 的完整加密存储功能：

1. **生成测试密钥**：创建 32 字节的 AES-256 密钥
2. **创建加密配置**：配置 EncryptionConfig 结构体
3. **创建加密存储**：使用 NewLevelDBStore 创建加密存储
4. **基本 Put/Get 操作**：测试加密存储和解密获取
5. **批量加密操作**：使用批量操作进行加密存储
6. **删除加密数据**：测试删除加密数据
7. **密码派生密钥**：使用 PBKDF2 从密码派生密钥
8. **查看加密配置**：获取和查看加密配置详情

---

## 8.2 运行示例

将上述代码保存为 `encryption_examples.go`，然后在项目根目录下创建一个新的示例目录并运行：

```bash
mkdir -p bible/examples/chapter08
cd bible/examples/chapter08
# 将上面的代码保存为 encryption_examples.go
go mod init encryption_examples
go mod tidy
go run encryption_examples.go
```

你将看到：

```
=== sfsDb 数据库实战 - 第 8 章：加密存储示例 ===

1. 生成测试密钥
   ✓ 密钥生成成功 (32字节)

2. 创建加密配置
   ✓ 加密配置创建成功
   - 算法: AES-256-GCM
   - 密钥长度: 32字节

3. 创建加密存储
   ✓ 加密存储创建成功

4. 测试基本的 Put 和 Get 操作
   ✓ 数据加密存储成功
   ✓ 数据解密获取成功
   原始数据: 这是需要加密的敏感数据
   解密数据: 这是需要加密的敏感数据
   ✓ 数据一致性验证通过

5. 测试批量加密操作
   ✓ 批量操作准备完成 (3条记录)
   ✓ 批量加密操作执行成功
   ✓ user_1_password 解密验证通过
   ✓ user_1_email 解密验证通过
   ✓ user_1_phone 解密验证通过

6. 测试删除加密数据
   ✓ 加密数据删除成功
   ✓ 验证数据已删除

7. 测试密码派生密钥 (PBKDF2)
   ✓ 使用密码派生密钥的加密存储创建成功
   - 使用 PBKDF2 密钥派生
   - 迭代次数: 100,000
   ✓ 密码派生密钥加密存储成功
   ✓ 密码派生密钥解密验证通过

8. 查看加密配置
   加密配置详情:
   - 启用: true
   - 算法: AES-256-GCM
   - 盐值长度: 0 字节

=== 加密存储示例完成！===
```

---

## 8.3 加密存储概述

数据安全是工业物联网和边缘计算的重要需求。sfsDb 提供内置的加密存储功能，支持 AES-256-GCM 加密算法。

加密存储的特点：
- **透明加密**：数据自动加密存储，透明解密读取
- **高性能**：使用解密缓存提高读取性能
- **密钥管理**：支持直接密钥和密码派生密钥
- **密钥轮换**：支持重新加密所有数据
- **完整功能**：支持 Put、Get、Delete、批量操作、迭代器、快照等

---

## 8.4 加密配置

### 8.4.1 EncryptionConfig 结构体

**示例代码**（来自 `storage/encryption.go`）：

```go
// EncryptionConfig 加密配置
type EncryptionConfig struct {
    // 是否启用加密
    Enabled bool `json:"enabled"`

    // 加密算法，默认AES-256-GCM
    Algorithm string `json:"algorithm"`

    // 主密钥，可以是直接的密钥或密码派生
    MasterKey []byte `json:"master_key,omitempty"`

    // 密码，用于派生密钥
    Password string `json:"password,omitempty"`

    // 盐值，用于密码派生
    Salt []byte `json:"salt,omitempty"`

    // 迭代次数，用于密码派生
    Iterations int `json:"iterations,omitempty"`
}
```

### 8.4.2 加密器接口

**示例代码**（来自 `storage/encryption.go`）：

```go
// Encryptor 加密器接口
type Encryptor interface {
    // 加密数据
    Encrypt(plaintext []byte) ([]byte, error)

    // 解密数据
    Decrypt(ciphertext []byte) ([]byte, error)

    // 获取加密算法
    Algorithm() string
}
```

---

## 8.5 AES-256-GCM 加密

### 8.5.1 AESGCMEncryptor 实现

**示例代码**（来自 `storage/encryption.go`）：

```go
// AESGCMEncryptor AES-256-GCM加密器实现
type AESGCMEncryptor struct {
    key       []byte
    algorithm string
    aesgcm    cipher.AEAD
}

// NewAESGCMEncryptor 创建新的AES-GCM加密器
func NewAESGCMEncryptor(key []byte) (*AESGCMEncryptor, error) {
    // 验证密钥长度
    if len(key) != 32 { // 256位密钥
        return nil, ErrInvalidKeyLength
    }

    // 预先创建并缓存 cipher 和 GCM 对象
    block, err := aes.NewCipher(key)
    if err != nil {
        return nil, ErrEncryptionFailed
    }

    aesgcm, err := cipher.NewGCM(block)
    if err != nil {
        return nil, ErrEncryptionFailed
    }

    return &amp;AESGCMEncryptor{
        key:       key,
        algorithm: "AES-256-GCM",
        aesgcm:    aesgcm,
    }, nil
}

// Encrypt 加密数据
func (e *AESGCMEncryptor) Encrypt(plaintext []byte) ([]byte, error) {
    // 生成随机nonce
    nonce := make([]byte, e.aesgcm.NonceSize())
    if _, err := io.ReadFull(rand.Reader, nonce); err != nil {
        return nil, ErrEncryptionFailed
    }

    // 加密数据
    ciphertext := e.aesgcm.Seal(nonce, nonce, plaintext, nil)
    return ciphertext, nil
}

// Decrypt 解密数据
func (e *AESGCMEncryptor) Decrypt(ciphertext []byte) ([]byte, error) {
    nonceSize := e.aesgcm.NonceSize()

    // 检查密文长度
    if len(ciphertext) &lt; nonceSize {
        return nil, ErrDecryptionFailed
    }

    // 提取nonce和密文
    nonce, ciphertext := ciphertext[:nonceSize], ciphertext[nonceSize:]

    // 解密数据
    plaintext, err := e.aesgcm.Open(nil, nonce, ciphertext, nil)
    if err != nil {
        return nil, ErrDecryptionFailed
    }

    return plaintext, nil
}
```

### 8.5.2 测试加密解密

**测试代码**（来自 `storage/encryption_test.go`）：

```go
func TestAESGCMEncryptorEncryptDecrypt(t *testing.T) {
    key := make([]byte, 32)
    for i := range key {
        key[i] = byte(i)
    }

    encryptor, err := NewAESGCMEncryptor(key)
    if err != nil {
        t.Fatalf("Failed to create encryptor: %v", err)
    }

    testData := []struct {
        name string
        data []byte
    }{
        {"Empty data", []byte{}},
        {"Small data", []byte("hello world")},
        {"Large data", bytes.Repeat([]byte("a"), 10000)},
        {"Binary data", []byte{0x00, 0x01, 0xFF, 0xFE}},
    }

    for _, tt := range testData {
        t.Run(tt.name, func(t *testing.T) {
            ciphertext, err := encryptor.Encrypt(tt.data)
            if err != nil {
                t.Fatalf("Encryption failed: %v", err)
            }

            if bytes.Equal(ciphertext, tt.data) {
                t.Fatalf("Ciphertext should not equal plaintext")
            }

            plaintext, err := encryptor.Decrypt(ciphertext)
            if err != nil {
                t.Fatalf("Decryption failed: %v", err)
            }

            if !bytes.Equal(plaintext, tt.data) {
                t.Fatalf("Decrypted data doesn't match original: got %x, want %x", plaintext, tt.data)
            }
        })
    }
}
```

---

## 8.6 密钥派生 (PBKDF2)

### 8.6.1 DeriveKey 函数

**示例代码**（来自 `storage/encryption.go`）：

```go
// DeriveKey 使用PBKDF2派生密钥
func DeriveKey(password, salt []byte, iterations int) ([]byte, error) {
    // 设置默认值
    if iterations &lt;= 0 {
        iterations = 100000
    }

    if len(salt) == 0 {
        // 生成随机盐值
        salt = make([]byte, 16)
        if _, err := io.ReadFull(rand.Reader, salt); err != nil {
            return nil, err
        }
    }

    // 派生32字节（256位）密钥
    key := pbkdf2.Key(password, salt, iterations, 32, sha256.New)
    return key, nil
}
```

### 8.6.2 测试密钥派生

**测试代码**（来自 `storage/encryption_test.go`）：

```go
func TestDeriveKey(t *testing.T) {
    tests := []struct {
        name        string
        password    []byte
        salt        []byte
        iterations  int
        expectError bool
    }{
        {
            name:        "All parameters provided",
            password:    []byte("test_password"),
            salt:        []byte("test_salt_123456"),
            iterations:  50000,
            expectError: false,
        },
        {
            name:        "No salt, should generate",
            password:    []byte("test_password"),
            salt:        nil,
            iterations:  100000,
            expectError: false,
        },
        {
            name:        "No iterations, should use default",
            password:    []byte("test_password"),
            salt:        []byte("test_salt_123456"),
            iterations:  0,
            expectError: false,
        },
        {
            name:        "Negative iterations, should use default",
            password:    []byte("test_password"),
            salt:        []byte("test_salt_123456"),
            iterations:  -100,
            expectError: false,
        },
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            key, err := DeriveKey(tt.password, tt.salt, tt.iterations)
            if tt.expectError &amp;&amp; err == nil {
                t.Fatalf("Expected error, got nil")
            }
            if !tt.expectError &amp;&amp; err != nil {
                t.Fatalf("Unexpected error: %v", err)
            }
            if !tt.expectError {
                if len(key) != 32 {
                    t.Fatalf("Expected key length 32, got %d", len(key))
                }
            }
        })
    }
}
```

---

## 8.7 加密存储包装器

### 8.7.1 EncryptedStoreWrapper 结构

**示例代码**（来自 `storage/encryption.go`）：

```go
// EncryptedStoreWrapper 加密存储包装器
type EncryptedStoreWrapper struct {
    // 底层存储
    underlyingStore Store

    // 加密配置和加密器（使用 atomic.Value 保证并发安全）
    state atomic.Value

    // 解密缓存（使用 sync.Map，无需手动锁）
    decryptionCache sync.Map

    // 缓存大小（原子计数器）
    cacheSize int64

    // 缓存最大条目数
    maxCacheEntries int
}
```

### 8.7.2 基本操作

**示例代码**（来自 `storage/encryption.go`）：

```go
// Get 获取并解密数据
func (es *EncryptedStoreWrapper) Get(key []byte) ([]byte, error) {
    cacheKey := string(key)
    now := time.Now().UnixNano()

    // 从缓存获取
    if v, ok := es.decryptionCache.Load(cacheKey); ok {
        entry := v.(*cacheEntry)

        // 更新访问时间
        newEntry := &amp;cacheEntry{
            value:      entry.value,
            lastAccess: now,
        }
        es.decryptionCache.Store(cacheKey, newEntry)

        return entry.value, nil
    }

    // 从底层存储获取加密数据
    encryptedValue, err := es.underlyingStore.Get(key)
    if err != nil {
        return nil, err
    }

    // 使用 atomic.Load 安全访问 encryptor
    s := es.state.Load().(*state)
    encryptor := s.encryptor

    // 解密数据
    value, err := encryptor.Decrypt(encryptedValue)
    if err != nil {
        return nil, err
    }

    // 存入缓存
    es.evictLRU()

    // 检查是否在 evict 期间被其他 goroutine 添加了
    if _, loaded := es.decryptionCache.LoadOrStore(cacheKey, &amp;cacheEntry{
        value:      value,
        lastAccess: now,
    }); !loaded {
        atomic.AddInt64(&amp;es.cacheSize, 1)
    }

    return value, nil
}

// Put 加密存储数据
func (es *EncryptedStoreWrapper) Put(key, value []byte) error {
    // 使用 atomic.Load 安全访问 encryptor
    s := es.state.Load().(*state)
    encryptor := s.encryptor

    // 加密值
    encryptedValue, err := encryptor.Encrypt(value)
    if err != nil {
        return err
    }

    // 存储加密后的数据
    err = es.underlyingStore.Put(key, encryptedValue)
    if err != nil {
        return err
    }

    // 更新缓存
    cacheKey := string(key)
    now := time.Now().UnixNano()

    es.evictLRU()

    newEntry := &amp;cacheEntry{
        value:      value,
        lastAccess: now,
    }

    if _, loaded := es.decryptionCache.Swap(cacheKey, newEntry); !loaded {
        atomic.AddInt64(&amp;es.cacheSize, 1)
    }

    return nil
}
```

### 8.7.3 测试加密存储

**测试代码**（来自 `storage/encryption_test.go`）：

```go
func TestEncryptionBasic(t *testing.T) {
    // 生成测试密钥
    masterKey := make([]byte, 32)
    for i := range masterKey {
        masterKey[i] = byte(i)
    }

    // 创建加密配置
    encryptConfig := &amp;EncryptionConfig{
        Enabled:   true,
        Algorithm: "AES-256-GCM",
        MasterKey: masterKey,
    }

    // 创建加密存储
    encryptedStore, err := dbManager.NewLevelDBStore("./test_encrypted_db", nil, encryptConfig)
    if err != nil {
        t.Fatalf("Failed to create encrypted store: %v", err)
    }
    defer encryptedStore.Close()
    defer RemoveDir("./test_encrypted_db")

    // 测试基本的Put和Get操作
    testKey := []byte("test_key")
    testValue := []byte("test_value")

    // 存储加密数据
    err = encryptedStore.Put(testKey, testValue)
    if err != nil {
        t.Fatalf("Failed to put encrypted data: %v", err)
    }

    // 获取解密数据
    retrievedValue, err := encryptedStore.Get(testKey)
    if err != nil {
        t.Fatalf("Failed to get encrypted data: %v", err)
    }

    // 验证数据一致性
    if !bytes.Equal(retrievedValue, testValue) {
        t.Fatalf("Retrieved value does not match original: got %s, want %s", retrievedValue, testValue)
    }

    // 测试删除操作
    err = encryptedStore.Delete(testKey)
    if err != nil {
        t.Fatalf("Failed to delete encrypted data: %v", err)
    }

    // 验证数据已删除
    _, err = encryptedStore.Get(testKey)
    if err == nil {
        t.Fatal("Expected ErrNotFound after deletion, got nil")
    }
    if err != ErrNotFound {
        t.Fatalf("Expected ErrNotFound after deletion, got %v", err)
    }
}
```

---

## 8.8 密钥轮换

### 8.8.1 ReEncrypt 函数

**示例代码**（来自 `storage/encryption.go`）：

```go
// ReEncrypt 重新加密所有数据
func (es *EncryptedStoreWrapper) ReEncrypt(newKey []byte) error {
    // 创建新的加密器
    newEncryptor, err := NewAESGCMEncryptor(newKey)
    if err != nil {
        return err
    }

    // 先读取当前的 state
    s := es.state.Load().(*state)
    currentEncryptor := s.encryptor
    currentConfig := s.config

    // 遍历所有数据
    iter := es.underlyingStore.Iterator(nil, nil)
    defer iter.Release()

    // 使用事务批量处理
    batch := es.underlyingStore.GetBatch()
    defer batch.Reset()

    for iter.First(); iter.Valid(); iter.Next() {
        key := iter.Key()
        encryptedValue := iter.Value()

        // 解密旧数据
        plaintext, err := currentEncryptor.Decrypt(encryptedValue)
        if err != nil {
            return err
        }

        // 使用新密钥加密
        newEncryptedValue, err := newEncryptor.Encrypt(plaintext)
        if err != nil {
            return err
        }

        // 更新批次
        batch.Put(key, newEncryptedValue)
    }

    // 提交批次
    if err := es.underlyingStore.WriteBatch(batch); err != nil {
        return err
    }

    // 创建新的 config
    newConfig := &amp;EncryptionConfig{
        Enabled:    currentConfig.Enabled,
        Algorithm:  currentConfig.Algorithm,
        MasterKey:  append([]byte(nil), newKey...),
        Password:   currentConfig.Password,
        Salt:       append([]byte(nil), currentConfig.Salt...),
        Iterations: currentConfig.Iterations,
    }

    // 更新 state（使用 atomic.Store 保证原子性）
    es.state.Store(&amp;state{
        encryptor: newEncryptor,
        config:    newConfig,
    })

    // 清空缓存
    es.decryptionCache = sync.Map{}
    atomic.StoreInt64(&amp;es.cacheSize, 0)

    return nil
}
```

### 8.8.2 测试密钥轮换

**测试代码**（来自 `storage/encryption_test.go`）：

```go
func TestEncryptionReEncrypt(t *testing.T) {
    // 生成初始密钥
    oldKey := make([]byte, 32)
    for i := range oldKey {
        oldKey[i] = byte(i)
    }

    // 生成新密钥
    newKey := make([]byte, 32)
    for i := range newKey {
        newKey[i] = byte(255 - i)
    }

    // 创建初始加密配置
    oldEncryptConfig := &amp;EncryptionConfig{
        Enabled:   true,
        Algorithm: "AES-256-GCM",
        MasterKey: oldKey,
    }

    // 创建加密存储
    encryptedStore, err := dbManager.NewLevelDBStore("./test_encrypted_reencrypt_db", nil, oldEncryptConfig)
    if err != nil {
        t.Fatalf("Failed to create encrypted store: %v", err)
    }
    defer encryptedStore.Close()
    defer RemoveDir("./test_encrypted_reencrypt_db")

    // 存储测试数据
    testData := map[string]string{
        "key1": "value1",
        "key2": "value2",
    }

    for k, v := range testData {
        err = encryptedStore.Put([]byte(k), []byte(v))
        if err != nil {
            t.Fatalf("Failed to put data: %v", err)
        }
    }

    // 执行密钥轮换
    encryptedWrapper, ok := encryptedStore.(*EncryptedStoreWrapper)
    if !ok {
        t.Fatal("Failed to convert to EncryptedStoreWrapper")
    }

    err = encryptedWrapper.ReEncrypt(newKey)
    if err != nil {
        t.Fatalf("Failed to re-encrypt data: %v", err)
    }

    // 验证数据仍然可以正确读取
    for k, v := range testData {
        retrievedValue, err := encryptedStore.Get([]byte(k))
        if err != nil {
            t.Fatalf("Failed to get data after re-encryption: %v", err)
        }

        if !bytes.Equal(retrievedValue, []byte(v)) {
            t.Fatalf("Retrieved value after re-encryption does not match original: got %s, want %s", retrievedValue, v)
        }
    }
}
```

---

## 8.9 安全最佳实践

1. **密钥长度**：使用 32 字节（256 位）密钥
2. **密钥保管**：妥善保管主密钥，不要提交到版本控制
3. **密码强度**：使用强密码进行密钥派生
4. **迭代次数**：PBKDF2 迭代次数建议 ≥ 100,000
5. **定期轮换**：定期轮换加密密钥
6. **盐值随机**：密码派生时使用随机盐值
7. **缓存清理**：定期清理解密缓存（内置 LRU 自动处理）

---

## 8.10 小结

本章我们通过项目的实际代码深入学习了 sfsDb 的加密存储功能：

- **加密配置**：理解 EncryptionConfig 结构体的各个字段
- **AES-256-GCM**：使用 AESGCMEncryptor 进行加密解密
- **密钥派生**：使用 PBKDF2 从密码派生密钥
- **加密存储包装器**：使用 EncryptedStoreWrapper 进行透明加密
- **解密缓存**：使用 LRU 缓存提高读取性能
- **密钥轮换**：使用 ReEncrypt() 重新加密所有数据
- **安全最佳实践**：了解如何安全地使用加密功能

在下一章中，我们将学习工业物联网实战。


---

**本书版本**：1.0.0  
**最后更新**：2026-03-11  
**sfsDb** - 以工业物联网边缘计算为核心场景的高性能嵌入式数据库！🚀  
**技术栈** - Go、leveldb。纯golang实现。
**项目地址**：[GitHub](https://github.com/liaoran123/sfsDb)  
**GitCode 镜像**：[GitCode](https://gitcode.com/liuyun258369/sfsDb)