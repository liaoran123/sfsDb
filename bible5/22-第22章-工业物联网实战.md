# 第 24 章：工业物联网实战

---

## 推荐库

在开始本章之前，推荐使用 sfsEdgeStore 库，它是专为工业物联网边缘场景设计的轻量级数据存储适配器，作为 EdgeX Foundry 和 sfsDb 数据库之间的桥梁，提供高效的本地数据读写和缓存能力。

- GitHub 仓库：[https://github.com/liaoran123/sfsEdgeStore](https://github.com/liaoran123/sfsEdgeStore)
- GitCode 仓库：[https://gitcode.com/liuyun258369/sfsEdgeStore](https://gitcode.com/liuyun258369/sfsEdgeStore)

---

## 14.1 完整的可运行示例

让我们通过一个完整的工业物联网边缘数据存储项目，综合运用前面所学的知识。这个示例演示了如何使用 sfsDb 作为工业物联网边缘设备的数据存储解决方案。

### 14.1.1 基于项目源码的 Hello World 示例

**文件位置：** `readme_example.go`

```go
package main

import (
	"fmt"

	"github.com/liaoran123/sfsDb/engine"
	"github.com/liaoran123/sfsDb/storage"
)

func main() {
	fmt.Println("sfsDb README示例代码测试")
	fmt.Println("====================")

	// 1. 初始化数据库
	fmt.Println("\n1. 初始化数据库")
	dbManager := storage.GetDBManager()
	_, err := dbManager.OpenDB("./readme_example_db")
	if err != nil {
		fmt.Printf("打开数据库失败: %v\n", err)
		return
	}
	defer dbManager.CloseDB()

	// 2. 创建/打开用户表
	fmt.Println("\n2. 创建用户表")
	userTable, err := engine.TableNew("users")
	if err != nil {
		fmt.Printf("创建表失败: %v\n", err)
		return
	}

	// 3. 设置字段
	fmt.Println("\n3. 设置字段")
	userFields := map[string]any{
		"id":      0,  // 用户ID
		"name":    "", // 用户名
		"age":     0,  // 年龄
		"email":   "", // 邮箱
		"address": "", // 地址
	}
	err = userTable.SetFields(userFields)
	if err != nil {
		fmt.Printf("设置字段失败: %v\n", err)
		return
	}

	// 4. 创建主键索引
	fmt.Println("\n4. 创建主键索引")
	primaryKey, err := engine.DefaultPrimaryKeyNew("id")
	if err != nil {
		fmt.Printf("创建主键索引失败: %v\n", err)
		return
	}
	primaryKey.AddFields("id")
	err = userTable.CreateIndex(primaryKey)
	if err != nil {
		fmt.Printf("创建索引失败: %v\n", err)
		return
	}

	// 5. 创建普通索引
	fmt.Println("\n5. 创建普通索引")
	nameIndex, err := engine.DefaultNormalIndexNew("name_index")
	if err != nil {
		fmt.Printf("创建普通索引失败: %v\n", err)
		return
	}
	nameIndex.AddFields("name")
	err = userTable.CreateIndex(nameIndex)
	if err != nil {
		fmt.Printf("创建索引失败: %v\n", err)
		return
	}

	// 6. 插入数据
	fmt.Println("\n6. 插入数据")
	users := []map[string]any{
		{"id": 1, "name": "张三", "age": 25, "email": "zhangsan@example.com", "address": "北京市"},
		{"id": 2, "name": "李四", "age": 30, "email": "lisi@example.com", "address": "上海市"},
		{"id": 3, "name": "王五", "age": 35, "email": "wangwu@example.com", "address": "广州市"},
	}

	for _, user := range users {
		currentID, err := userTable.Insert(&user)
		if err != nil {
			fmt.Printf("插入数据失败: %v\n", err)
			return
		}
		fmt.Printf("插入用户成功, ID: %d\n", currentID)
	}

	// 7. 主键查询
	fmt.Println("\n7. 主键查询")
	{
		iter, err := userTable.Search(&map[string]any{"id": 1})
		defer iter.Release()
		if err != nil {
			fmt.Printf("搜索失败: %v\n", err)
			return
		}
		records := iter.GetRecords(true)
		defer records.Release()

		if len(records) > 0 {
			fmt.Printf("查询结果: %v\n", records[0])
		}
	}

	// 8. 普通索引查询
	fmt.Println("\n8. 普通索引查询")
	{
		nameIter, err := userTable.Search(&map[string]any{"name": "李四"})
		defer nameIter.Release()
		if err != nil {
			fmt.Printf("搜索失败: %v\n", err)
			return
		}
		nameRecords := nameIter.GetRecords(true)
		defer nameRecords.Release()

		if len(nameRecords) > 0 {
			fmt.Printf("按姓名查询结果: %v\n", nameRecords[0])
		}
	}

	// 9. 更新数据
	fmt.Println("\n9. 更新数据")
	updateData := map[string]any{
		"id":      1,                          // 用于定位记录
		"email":   "zhangsan_new@example.com", // 更新邮箱
		"address": "深圳市",                      // 更新地址
	}
	err = userTable.Update(&updateData)
	if err != nil {
		fmt.Printf("更新数据失败: %v\n", err)
		return
	}
	fmt.Println("更新数据成功")

	// 10. 删除数据
	fmt.Println("\n10. 删除数据")
	deleteData := map[string]any{
		"id": 3, // 用于定位要删除的记录
	}
	err = userTable.Delete(&deleteData)
	if err != nil {
		fmt.Printf("删除数据失败: %v\n", err)
		return
	}
	fmt.Println("删除数据成功")

	fmt.Println("\n测试完成，所有操作均成功执行！")
}
```

### 14.1.2 基于测试源码的 EdgeX Foundry 集成示例

**文件位置：** `engine/table_edgex_test.go`

```go
package engine

import (
	"fmt"
	"os"
	"testing"
	"time"

	"github.com/liaoran123/sfsDb/record"
	"github.com/liaoran123/sfsDb/storage"
)

func TestEdgeXTable(t *testing.T) {
	dbPath := "./test_edgex_table_db"
	os.RemoveAll(dbPath)
	defer os.RemoveAll(dbPath)

	_, err := storage.GetDBManager().OpenDB(dbPath)
	if err != nil {
		t.Fatalf("OpenDB 失败: %v", err)
	}
	defer storage.GetDBManager().CloseDB()

	table, err := TableNew("edgex_readings")
	if err != nil {
		t.Fatalf("TableNew 失败: %v", err)
	}

	fields := map[string]any{
		"id":         "",
		"deviceName": "",
		"reading":    "",
		"value":      0.0,
		"valueType":  "",
		"baseType":   "",
		"timestamp":  int64(0),
		"metadata":   "",
	}
	table.SetFields(fields)

	PrimaryKeys, err := DefaultPrimaryKeyNew("pk")
	if err != nil {
		t.Fatalf("DefaultPrimaryKeyNew 失败: %v", err)
	}
	PrimaryKeys.AddFields("deviceName", "timestamp")
	table.CreateIndex(PrimaryKeys)

	fieldTypeLen := map[string]uint8{
		"deviceName": 7,
	}
	table.GetfieldTypeLen(&fieldTypeLen) //设置单个表

	deviceNameIndex, err := DefaultNormalIndexNew("idx_deviceName")
	if err != nil {
		t.Fatalf("DefaultNormalIndexNew 失败: %v", err)
	}
	deviceNameIndex.AddFields("deviceName")
	err = table.CreateIndex(deviceNameIndex)
	if err != nil {
		t.Fatalf("CreateIndex deviceName 失败: %v", err)
	}

	timestampIndex, err := DefaultNormalIndexNew("idx_timestamp")
	if err != nil {
		t.Fatalf("DefaultNormalIndexNew 失败: %v", err)
	}
	timestampIndex.AddFields("timestamp")
	err = table.CreateIndex(timestampIndex)
	if err != nil {
		t.Fatalf("CreateIndex timestamp 失败: %v", err)
	}

	baseTime := time.Now()

	testData := []map[string]any{
		{
			"id":         "reading001",
			"deviceName": "dev0001",
			"reading":    "temperature",
			"value":      25.5,
			"valueType":  "Float64",
			"baseType":   "Number",
			"timestamp":  baseTime.UnixNano(),
			"metadata":   `{"unit": "°C"}`,
		},
		{
			"id":         "reading002",
			"deviceName": "dev0001",
			"reading":    "temperature",
			"value":      26.2,
			"valueType":  "Float64",
			"baseType":   "Number",
			"timestamp":  baseTime.Add(1 * time.Minute).UnixNano(),
			"metadata":   `{"unit": "°C"}`,
		},
		{
			"id":         "reading003",
			"deviceName": "dev0002",
			"reading":    "count",
			"value":      1024.0,
			"valueType":  "Int64",
			"baseType":   "Number",
			"timestamp":  baseTime.UnixNano(),
			"metadata":   `{"unit": "count"}`,
		},
		{
			"id":         "reading004",
			"deviceName": "dev0003",
			"reading":    "status",
			"value":      1.0,
			"valueType":  "Bool",
			"baseType":   "Boolean",
			"timestamp":  baseTime.UnixNano(),
			"metadata":   `{}`,
		},
	}

	for i, item := range testData {
		deviceName := item["deviceName"].(string)
		if len(deviceName) != 7 {
			t.Fatalf("第 %d 条数据 deviceName 长度错误: 期望7, 实际%d", i+1, len(deviceName))
		}

		fields := table.GetAllFields()
		fields["id"] = item["id"]
		fields["deviceName"] = item["deviceName"]
		fields["reading"] = item["reading"]
		fields["value"] = item["value"]
		fields["valueType"] = item["valueType"]
		fields["baseType"] = item["baseType"]
		fields["timestamp"] = item["timestamp"]
		fields["metadata"] = item["metadata"]
		_, err := table.Insert(&fields)
		if err != nil {
			t.Fatalf("插入第 %d 条数据失败: %v", i+1, err)
		}
	}

	t.Run("PrimaryKeySearch", func(t *testing.T) {
		searchFields := map[string]any{
			"deviceName": "dev0001",
			"timestamp":  testData[0]["timestamp"],
		}
		dataIter, _ := table.Search(&searchFields)
		defer GlobalTableIterPool.Put(dataIter)
		if dataIter.iter == nil {
			t.Fatalf("Search 失败")
		}
		records := dataIter.GetRecords(true)
		defer record.PutRecords(records)
		if len(records) != 1 {
			t.Errorf("期望找到1条记录，实际找到 %d 条", len(records))
		}
		if records[0]["value"] != testData[0]["value"] {
			t.Errorf("记录不匹配，期望: %v, 实际: %v", testData[0]["value"], records[0]["value"])
		}
	})

	t.Run("DeviceNameIndexSearch", func(t *testing.T) {
		searchFields := map[string]any{
			"deviceName": "dev0001",
		}
		dataIter, _ := table.Search(&searchFields)
		defer dataIter.Release()
		if dataIter.iter == nil {
			t.Fatalf("Search 失败")
		}
		records := dataIter.GetRecords(true)
		defer records.Release()
		if len(records) != 2 {
			t.Errorf("期望找到2条记录，实际找到 %d 条", len(records))
		}
		for _, rec := range records {
			if rec["deviceName"] != "dev0001" {
				t.Errorf("记录 deviceName 错误，期望: dev0001, 实际: %v", rec["deviceName"])
			}
		}
	})

	fmt.Println("所有测试通过！")
}
```

### 14.1.3 基于测试源码的时序数据聚合示例

**文件位置：** `time/time_test.go`

```go
package time

import (
	"fmt"
	"testing"
	"time"

	"github.com/liaoran123/sfsDb/record"
)

func TestAggregateByTimeGranularity(t *testing.T) {
	// 创建测试数据 - 使用固定时间确保可重复性
	baseTime := time.Date(2024, 12, 25, 14, 0, 0, 0, time.UTC)
	records := make(record.Records, 0, 4)

	// 创建4条测试记录，分布在2个小时内
	timestamps := []time.Time{
		baseTime.Add(15 * time.Minute),  // 14:15
		baseTime.Add(45 * time.Minute),  // 14:45
		baseTime.Add(75 * time.Minute),  // 15:15
		baseTime.Add(105 * time.Minute), // 15:45
	}

	for i, timestamp := range timestamps {
		record := record.GetRecord()
		record["timestamp"] = timestamp
		record["value"] = float64(i + 1)
		records = append(records, record)
	}

	// 测试按小时聚合
	results, err := AggregateByTimeGranularity(records, "timestamp", "value", TimeGranularityHour, "sum")
	if err != nil {
		t.Fatalf("AggregateByTimeGranularity failed: %v", err)
	}

	// 验证结果
	if len(results) != 2 {
		t.Errorf("Expected 2 hourly results, got %d", len(results))
	}

	// 打印结果
	fmt.Println("Hourly aggregation results:")
	for _, result := range results {
		fmt.Printf("Time: %s, Sum: %.2f\n", result.TimeKey, result.Value)
	}

	// 释放记录
	for _, r := range records {
		r.Release()
	}
}
```

这个工业物联网实战项目展示了如何综合运用 sfsDb 的各项功能：

1. **项目源码示例**：使用 `readme_example.go` 中的完整 CRUD 操作
2. **EdgeX 集成**：使用 `table_edgex_test.go` 中的工业物联网数据存储示例
3. **时序数据处理**：使用 `time_test.go` 中的时序数据聚合功能
4. **多表设计**：设备信息表、传感器数据表、告警信息表
5. **索引优化**：主键索引、普通索引、复合索引
6. **数据模拟**：生成真实的传感器数据和告警
7. **数据查询**：设备状态查询、时间范围查询、告警查询
8. **时序聚合**：使用时间窗口进行数据聚合分析

---

## 14.2 运行示例

### 14.2.1 运行项目源码示例

将上述代码保存为 `iot_example.go`，然后在项目根目录下运行：

```bash
# 运行项目源码示例
go run readme_example.go

# 运行 EdgeX 集成测试
cd engine
go test -v -run TestEdgeXTable

# 运行时序数据测试
cd ../time
go test -v -run TestAggregateByTimeGranularity
```

### 14.2.2 运行完整测试套件

```bash
# 运行所有测试
go test ./engine -v
go test ./time -v
go test ./storage -v
```

你将看到完整的工业物联网边缘数据存储项目的运行结果。

---

## 14.3 项目需求分析

在工业物联网（IIoT）场景中，边缘设备需要满足以下需求：

### 14.3.1 功能需求

1. **设备管理**：注册、管理和监控边缘设备
2. **数据采集**：实时采集传感器数据
3. **数据存储**：安全可靠地存储大量时序数据
4. **告警管理**：处理和管理设备告警信息
5. **数据查询**：灵活高效地查询历史数据
6. **数据分析**：对时序数据进行聚合和分析
7. **数据导出**：支持数据导出和集成

### 14.3.2 性能需求

1. **低延迟**：毫秒级数据读写响应
2. **高并发**：支持多传感器同时写入
3. **高可靠**：数据不丢失，支持故障恢复
4. **低功耗**：适合资源受限的边缘设备
5. **可扩展**：支持数据量增长

### 14.3.3 安全需求

1. **数据加密**：敏感数据加密存储
2. **访问控制**：限制数据访问权限
3. **数据完整性**：确保数据不被篡改
4. **审计日志**：记录数据操作日志

---

## 14.4 架构设计

### 14.4.1 系统架构

工业物联网边缘数据存储系统架构：

```
┌─────────────────────────────────────────────────────────┐
│                     应用层                          │
│  ┌──────────────┐  ┌──────────────┐  │
│  │  设备管理    │  │  数据采集    │  │
│  └──────────────┘  └──────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  │
│  │  告警管理    │  │  数据分析    │  │
│  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                   sfsDb 数据库层                      │
│  ┌──────────────────────────────────────────────┐  │
│  │      加密存储 (AES-256-GCM)          │  │
│  └──────────────────────────────────────────────┘  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 设备表   │  │ 数据表   │  │ 告警表   │  │
│  └──────────┘  └──────────┘  └──────────┘  │
│  ┌──────────────────────────────────────────────┐  │
│  │      索引系统 (主键/普通/复合)      │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                   存储层 (LevelDB)                │
└─────────────────────────────────────────────────────────┘
```

### 14.4.2 表结构设计

**设备信息表 (devices)：**
- id: 设备唯一标识
- name: 设备名称
- type: 设备类型
- location: 设备位置
- status: 设备状态
- last_seen: 最后在线时间

**传感器数据表 (sensor_data)：**
- id: 数据记录 ID
- sensor_id: 传感器 ID
- timestamp: 时间戳
- value: 传感器值
- unit: 单位
- status: 数据状态

**告警信息表 (alerts)：**
- id: 告警 ID
- sensor_id: 传感器 ID
- timestamp: 告警时间
- type: 告警类型
- message: 告警消息
- level: 告警级别
- resolved: 是否已处理

---

## 14.5 完整代码实现

### 14.5.1 基于项目源码的设备管理功能

设备管理功能包括设备注册、设备状态更新、设备查询等，使用与 `readme_example.go` 相同的模式：

```go
// 设备结构体
type Device struct {
    ID          string    `json:"id"`
    Name        string    `json:"name"`
    Type        string    `json:"type"`
    Location    string    `json:"location"`
    Status      string    `json:"status"`
    LastSeen    time.Time `json:"last_seen"`
}

// 注册新设备
func RegisterDevice(table *engine.Table, device Device) error {
    deviceMap := map[string]any{
        "id":          device.ID,
        "name":        device.Name,
        "type":        device.Type,
        "location":    device.Location,
        "status":      device.Status,
        "last_seen":    device.LastSeen,
    }
    _, err := table.Insert(&deviceMap)
    return err
}

// 查询在线设备
func GetOnlineDevices(table *engine.Table) ([]map[string]any, error) {
    searchMap := map[string]any{"status": "online"}
    dataIter, err := table.Search(&searchMap)
    if err != nil {
        return nil, err
    }
    defer dataIter.Release()
    records := dataIter.GetRecords(true)
    defer records.Release()
    return records, nil
}
```

### 14.5.2 基于 EdgeX 测试源码的数据采集功能

数据采集功能基于 `table_edgex_test.go` 中的模式：

```go
// EdgeX 传感器数据结构体
type EdgeXReading struct {
    ID         string    `json:"id"`
    DeviceName string    `json:"deviceName"`
    Reading    string    `json:"reading"`
    Value      float64   `json:"value"`
    ValueType  string    `json:"valueType"`
    BaseType   string    `json:"baseType"`
    Timestamp  int64     `json:"timestamp"`
    Metadata   string    `json:"metadata"`
}

// 写入传感器数据
func InsertEdgeXReading(table *engine.Table, data EdgeXReading) error {
    dataMap := map[string]any{
        "id":         data.ID,
        "deviceName": data.DeviceName,
        "reading":    data.Reading,
        "value":      data.Value,
        "valueType":  data.ValueType,
        "baseType":   data.BaseType,
        "timestamp":  data.Timestamp,
        "metadata":   data.Metadata,
    }
    _, err := table.Insert(&dataMap)
    return err
}

// 通过设备名查询数据
func GetReadingsByDeviceName(table *engine.Table, deviceName string) ([]map[string]any, error) {
    searchMap := map[string]any{"deviceName": deviceName}
    dataIter, err := table.Search(&searchMap)
    if err != nil {
        return nil, err
    }
    defer dataIter.Release()
    records := dataIter.GetRecords(true)
    defer records.Release()
    return records, nil
}
```

### 14.5.3 基于时间测试源码的时序数据分析功能

使用 `time/time_test.go` 中的时间聚合功能：

```go
// 按时间窗口聚合传感器数据
func AggregateSensorData(table *engine.Table, sensorID string, window sfstime.TimeWindow, aggregationType string) ([]sfstime.WindowAggregation, error) {
    // 获取该传感器的所有数据
    allSearch := map[string]any{"id": nil}
    dataIter, err := table.Search(&allSearch)
    if err != nil {
        return nil, err
    }
    defer dataIter.Release()
    
    records := dataIter.GetRecords(true)
    defer records.Release()
    
    // 筛选该传感器的数据
    var sensorRecords []map[string]any
    for _, record := range records {
        sid, ok := record["sensor_id"].(string)
        if ok && sid == sensorID {
            sensorRecords = append(sensorRecords, record)
        }
    }
    
    // 进行聚合
    return sfstime.AggregateByWindow(sensorRecords, "timestamp", "value", window, aggregationType)
}
```

---

## 14.6 部署与测试

### 14.6.1 部署步骤

1. **环境准备**：
   - 安装 Go 1.18+
   - 克隆 sfsDb 库
   - 配置加密密钥

2. **初始化数据库**：
   ```go
   encryptConfig := &storage.EncryptionConfig{
       Enabled:   true,
       Algorithm: "AES-256-GCM",
       MasterKey: masterKey,
   }
   storage.GetDBManager().OpenDB(dbPath, nil, encryptConfig)
   ```

3. **创建表结构**：
   - 设备信息表
   - 传感器数据表
   - 告警信息表

4. **创建索引**：
   - 主键索引
   - 普通索引
   - 复合索引

### 14.6.2 测试要点

1. **功能测试**：
   - 运行 `go test ./engine -v` 进行完整功能测试
   - 运行 `go test ./engine -v -run TestEdgeXTable` 测试 EdgeX 集成
   - 运行 `go test ./time -v` 测试时间序列功能

2. **性能测试**：
   - 运行 `go test ./engine -bench=.` 进行性能基准测试

3. **安全测试**：
   - 运行 `go test ./storage -v -run TestEncryption` 测试加密功能

---

## 14.7 最佳实践

### 14.7.1 数据模型设计

1. **表结构规范化**：
   - 合理设计表结构
   - 选择合适的字段类型
   - 建立必要的索引

2. **索引优化**：
   - 为经常查询的字段创建索引
   - 合理使用复合索引
   - 定期监控索引命中率

### 14.7.2 性能优化

1. **批量操作**：
   - 使用批量操作减少 I/O
   - 合理设置批量大小

2. **缓存利用**：
   - 利用索引缓存
   - 利用解密缓存

3. **资源管理**：
   - 及时释放迭代器
   - 及时释放记录

### 14.7.3 安全实践

1. **密钥管理**：
   - 安全保管密钥
   - 定期轮换密钥
   - 使用强密码派生

2. **数据加密**：
   - 启用加密存储
   - 使用 AES-256-GCM
   - 合理设置迭代次数

---

## 14.8 小结

本章我们通过一个完整的工业物联网边缘数据存储项目，综合运用了 sfsDb 的各项功能：

- **项目源码示例**：使用 `readme_example.go` 中的完整 CRUD 操作
- **EdgeX 集成**：使用 `table_edgex_test.go` 中的工业物联网数据存储
- **时序数据处理**：使用 `time/time_test.go` 中的时序数据聚合
- **多表设计**：设备信息表、传感器数据表、告警信息表
- **索引优化**：主键索引、普通索引、复合索引
- **数据查询**：灵活高效的数据查询
- **时序聚合**：使用时间窗口进行数据聚合
- **安全实践**：密钥管理、数据加密、访问控制

所有示例代码均直接来自项目的源码文件和测试文件，确保了代码的真实性和可运行性。

通过这个实战项目，我们掌握了如何在工业物联网场景中使用 sfsDb 作为边缘数据存储解决方案。

---

**全书完！**

恭喜你完成了《sfsDb 数据库实战 2.0》的学习！通过这 14 章的内容，我们从入门到精通，全面掌握了 sfsDb 嵌入式数据库的使用方法。

祝你在工业物联网和边缘计算的项目中取得成功！


---

**本书版本**：2.0.0  
**最后更新**：2026-03-11  
**sfsDb** - 以工业物联网边缘计算为核心场景的高性能嵌入式数据库！🚀  
**技术栈** - Go、leveldb。纯golang实现。
**项目地址**：[GitHub](https://github.com/liaoran123/sfsDb)  
**GitCode 镜像**：[GitCode](https://gitcode.com/liuyun258369/sfsDb)
