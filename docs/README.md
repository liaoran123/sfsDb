# sfsDb 数据库使用文档

## 项目简介

sfsDb 是一个灵活、高效的嵌入式数据库，支持多种数据类型、索引类型和查询方式。它提供了简洁的 API 设计，易于集成到各种应用场景中。基于**服务导向的商业模式**，我们将重点放在国内市场的深耕上，通过提供专业服务和增值内容来实现商业价值。

## 语言选择

- [中文文档](./zh/README.md)
- [English Documentation](./en/README.md)

## 主要特性

1. **灵活的数据模型**：支持多种字段类型和动态字段
2. **强大的索引系统**：支持单主键、复合主键、普通索引和全文索引
3. **丰富的查询功能**：支持比较操作符、自定义匹配器和全文搜索
4. **易于使用的 API**：简洁的 API 设计，易于集成到各种应用场景
5. **高效的性能**：优化的存储结构和查询算法
6. **事务支持**：确保数据操作的原子性和一致性
7. **对象池机制**：优化内存使用和性能
8. **半结构化数据支持**：灵活处理复杂数据结构
9. **管理工具库**：提供数据库监控、配置管理、备份恢复和性能分析功能
10. **时序数据处理**：内置 time 包，支持时间粒度处理、时间窗口计算、数据聚合和时间戳转换等时序数据相关功能
11. **加密存储**：支持 AES-256-GCM 加密、PBKDF2 密钥派生和密钥轮换功能

## 服务体系

### 服务分级
- **社区服务**：通过GitHub Issues、微信群等渠道提供基础支持（免费）
- **专业服务**：提供技术支持、问题排查、性能优化等（按次或包月收费）
- **企业服务**：提供定制开发、现场部署、培训认证等（项目制收费）

### 年度支持服务包

- **基础运维包**：5000元/年
  - **适用对象**：个人开发者、初创项目、非关键业务测试环境
  - **服务内容**：远程自助诊断工具+工作日远程协助、5次轻量级技术咨询（单次≤30分钟）、24小时故障响应（邮件/工单）、基础文档与知识库权限
  - **节点限制**：支持1-5个边缘节点
  - **价值点**：低成本入门，解决基础适配与配置问题

- **高级保障包**：15000元/年
  - **适用对象**：中型企业、生产环境、高可用性要求场景
  - **服务内容**：7×24小时远程监控与协助、无限次技术咨询（含复杂问题排查）、4小时紧急响应（P1级故障）、每月1次远程健康检查（含数据库碎片、锁状态分析）、2次现场支持（差旅费实报实销，单次≤2人天）
  - **节点限制**：支持1-50个边缘节点
  - **价值点**：生产环境兜底，通过健康检查预防边缘端数据损坏风险

- **企业尊享包**：50000元/年起
  - **适用对象**：大型企业、关键业务系统、多节点分布式边缘集群
  - **服务内容**：7×24小时专属工程师驻场（远程）、不限次现场支持（差旅费实报实销）、定制化远程管理方案（含数据同步策略、断网续传逻辑设计）、SLA协议（99.9%可用性承诺）、优先获取新版本测试权限
  - **节点限制**：支持50个以上边缘节点
  - **价值点**：专家级贴身服务，解决复杂边缘-云端数据协同难题

### 专项服务与附加项

- **现场支持差旅费**：实报实销，标准如下：交通费（高铁二等座/飞机经济舱）、住宿费（一线城市≤500元/天，二线城市≤400元/天）、餐补（100元/天）
- **紧急故障加急费**：非工作时间（夜间22:00-次日8:00）紧急响应，加收50%服务费
- **硬件适配费**：针对非标准边缘设备（如特种工业网关）的驱动开发，按1500元/人天单独报价
- **健康检查**：5000元/次，提供系统健康检查报告和优化建议
- **灾备方案**：10000元起，根据客户规模定制
- **性能优化**：8000元/次，针对特定场景的性能调优
- **定制开发**：20000元/人月起，高难度任务（如内核修改、跨平台移植）25000元/人月以上

## 快速开始

### 1. 安装

```bash
go get github.com/liaoran123/sfsDb
```

### 2. 基本使用

```go
package main

import (
    "fmt"
    "github.com/liaoran123/sfsDb/engine"
)

func main() {
    // 创建表
    table, err := engine.TableNew("users")
    if err != nil {
        panic(err)
    }
    
    // 设置字段
    fields := map[string]any{
        "id":   0,     // 自动增值主键
        "name": "",    // 字符串类型
        "age":  0,     // 整数类型
    }
    err = table.SetFields(fields)
    if err != nil {
        panic(err)
    }
    
    // 插入数据
    user := map[string]any{
        "name": "Alice",
        "age":  30,
    }
    id, err := table.Insert(&user)
    if err != nil {
        panic(err)
    }
    fmt.Printf("插入成功，ID: %v\n", id)
    
    // 查询数据
    searchData := map[string]any{"name": "Alice"}
    iter := table.Search(&searchData)   
    if iter != nil {
        defer iter.Release()
    }
    
    records := iter.GetRecords(true)
    if records != nil {
        defer records.Release()
    }
    
    for _, record := range records {
        fmt.Printf("记录: %v\n", record)
    }
}
```

## 文档结构

### 基础功能
- 数据库初始化
- 创建表与设置字段
- 插入数据
- 查询数据
- 删除记录
- 管理工具库使用

### 高级功能
- 主键管理
- 索引管理
- 全文搜索
- 字段修改
- 事务管理
- 其他功能

### 优化与最佳实践
- 对象池与内存管理
- 半结构化数据支持
- 最佳实践
- 常见问题
- 总结

### 工具与命令
- [CLI命令参考](./cli_commands.md) - 命令行工具使用指南
- [CLI Command Reference](./cli_commands.en.md) - Command-line tool usage guide

## 贡献指南

欢迎贡献代码和文档！请参考 [CONTRIBUTING.md](https://github.com/liaoran123/sfsDb/blob/main/CONTRIBUTING.md) 了解如何参与项目。

## 联系我们

如果您有任何问题或建议，欢迎通过以下方式联系我们：

- **邮箱**: sfsweb@qq.com

## 许可证

sfsDb 采用双许可证模式：

### 核心引擎 - MIT 许可证
- **开源免费**: 核心数据库引擎使用 MIT 开源许可证
- **商业友好**: 允许自由使用、修改和商业分发
- **简洁灵活**: 许可证文本简洁，限制少，易于理解和使用

### 企业版插件 - 商业许可证
- **高级功能**: 企业版插件（如高级安全特性、多节点协调等）使用商业许可证
- **支持服务**: 包含专业技术支持、定期更新和企业级功能